---
description: "ya make build and test commands"
alwaysApply: false
---

`./ya make folder` - build
`./ya make -tA folder` - test all
`./ya make -tA folder -F *name*` - filter tests

No `-j`. No force rebuild. Use `2>&1 | tail` for output.
